json.extract! whatsapp_stat, :id, :phrase, :hugo_frequency, :sarah_frequency, :months_frequency, :hugo_months_frequency, :sarah_months_frequency, :created_at, :updated_at
json.url whatsapp_stat_url(whatsapp_stat, format: :json)
